{ config, pkgs, lib, ... }:
{
	# ========================================================================
	# HYPRLAND MODULE - System-level configuration
	# ========================================================================
	# NOTE: Main Hyprland config is in home/hyprland.nix via Home Manager.
	# This module enables Hyprland at the system level (done in desktop.nix).
	# Kept as a stub for future system-level Hyprland overrides.
}
